<Page x:Class="ArchiveApp.ReportOptionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Параметры отчета" d:DesignHeight="450" d:DesignWidth="400" Background="White">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>
    <Grid Margin="10" Name="MainGrid" MouseDown="MainGrid_MouseDown" KeyDown="MainGrid_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Формат отчета -->
        <GroupBox Header="Формат отчета" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel>
                <RadioButton x:Name="WordRadio" Content="Word" IsChecked="True" Margin="5"/>
                <RadioButton x:Name="ExcelRadio" Content="Excel" Margin="5"/>
                <RadioButton x:Name="PdfRadio" Content="PDF" Margin="5"/>
            </StackPanel>
        </GroupBox>

        <!-- Выбор таблиц -->
        <GroupBox Header="Таблицы для включения" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel>
                <CheckBox x:Name="AllTablesCheckBox" Content="Все таблицы" Margin="5" Checked="AllTablesCheckBox_Checked" Unchecked="AllTablesCheckBox_Unchecked"/>
                <CheckBox x:Name="DocumentsCheckBox" Content="Документы" Margin="5"/>
                <CheckBox x:Name="RequestsCheckBox" Content="Запросы" Margin="5"/>
                <CheckBox x:Name="UsersCheckBox" Content="Пользователи" Margin="5"/>
                <CheckBox x:Name="RegCardsCheckBox" Content="Регистрационные карты" Margin="5"/>
            </StackPanel>
        </GroupBox>

        <!-- Период отчета -->
        <GroupBox Header="Период отчета" Grid.Row="2" Margin="0,0,0,10" Visibility="{Binding IsFullReport, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <DatePicker x:Name="StartDatePicker" Margin="5" SelectedDate="{Binding StartDate}"/>
                <DatePicker x:Name="EndDatePicker" Margin="5" SelectedDate="{Binding EndDate}"/>
            </StackPanel>
        </GroupBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Создать" Width="100" Margin="5" Click="CreateButton_Click" Background="#bae3e8"/>
        </StackPanel>
    </Grid>
</Page>