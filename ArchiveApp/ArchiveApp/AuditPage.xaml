<Page x:Class="ArchiveApp.AuditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Аудит действий" 
      d:DesignHeight="550" d:DesignWidth="900">

    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Текущая сессия" Width="160" Height="35" Margin="5" Click="CurrentSession_Click" Background="#bae3e8"/>
            <Button Content="Полная история" Width="160" Height="35" Margin="5" Click="FullHistory_Click" Background="#bae3e8"/>
            <Button Content="Очистить сессию" Width="160" Height="35" Margin="5" Click="ClearSession_Click" Background="#ffdddd"/>
            <Button Content="Очистить историю" Width="160" Height="35" Margin="5" Click="ClearHistory_Click" Background="#ff9999"/>
        </StackPanel>

        <DataGrid x:Name="AuditDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="All" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата и время" Binding="{Binding Timestamp, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="160"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding UserLogin}" Width="130"/>
                <DataGridTextColumn Header="Действие" Binding="{Binding Action}" Width="220"/>
                <DataGridTextColumn Header="Объект" Binding="{Binding EntityType}" Width="110"/>
                <DataGridTextColumn Header="Подробности" Binding="{Binding Details}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>